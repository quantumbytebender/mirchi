// there are some user types that are not used in the app, but are used in the database
enum UserType {
  admin,
  projectManager,
  teamLead,
  developer,
  tester,
  designer,
  client,
  guest,
}

